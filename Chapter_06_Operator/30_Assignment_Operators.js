// assignment operators are used to assign values to variables. They include the basic assignment operator (=) and compound assignment operators (like +=, -=, *=, /=, etc.) that perform an operation and assignment in one step.

let x = 10; // basic assignment operator
x=20;
x = 45;
console.log(x);

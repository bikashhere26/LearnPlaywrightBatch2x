// while loop exercise

let attempt = 0; // Initialize the attempt variable

while (attempt < 3) { // Condition to check if attempts are less than 3
    console.log("Attempt " + (attempt + 1)); // Log the current attempt number
    attempt++; // Increment the attempt variable
}

let modi = 1;
while (modi <= 15) {
    console.log("modi is a successful leader");
    modi++;
}